# analysis-code

This folder contains two R script files with both initial and full statistical analysis.

The initial analysis is to explore an initial model with some interesting variables, and the full analysis is to use machine learning and cross-validation to generate a proper model.

The code produces a few tables and figures, which are saved in the appropriate `results` sub-folder.